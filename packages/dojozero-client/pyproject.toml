[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dojozero-client"
version = "0.1.0"
description = "Python SDK for DojoZero external agents."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "DojoZero Team"}
]
dependencies = [
    "httpx>=0.28.0",
    "PyYAML>=6.0",
]
